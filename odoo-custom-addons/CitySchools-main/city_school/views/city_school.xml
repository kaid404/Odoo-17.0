<odoo>
    <record id="view_op_student_form_inherit" model="ir.ui.view">
        <field name="name">account.move.form.inherit</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <header>
                <button name="%(city_school.report_city_school_detail)d" string="Print Fee Receipt" type="action" class="btn-primary" invisible="payment_state != 'paid'"/>
<!--                <field name="payment_create_date" invisible="1"/>-->
<!--                <field name="payment_create_user" invisible="1"/>-->
            </header>
        </field>
    </record>
</odoo>
